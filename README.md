🖥️ Simple Bash-Like Console

A small learning project that implements a minimal shell with a built-in text editor.

✨ Features

/bin/shell as the main executable

Basic shell commands

ls — list directory contents

cd — change directory

Built-in lightweight text editor

Ctrl-S → save

Ctrl-Q → quit
